rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny. Every collection gets its own explicit rule, added
    // alongside the feature that needs it (see CLAUDE.md), scoped to
    // request.auth.uid.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
